<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.eCommerceV3.Views.ShopManagementView"
             Title="ShopManagementView">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition /><!--STORE-->
            <ColumnDefinition /><!--USER SHOPPING CART-->
        </Grid.ColumnDefinitions>
        <!--STORE-->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ListView
                ItemsSource="{Binding Inventory}"
                SelectedItem="{Binding SelectedItem}"
                >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width ="4*"/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" Text="{Binding Id}" />
                                <Label Grid.Column="1" Text="{Binding Product.Name}" />
                                <Label Grid.Column="2" Text="Quantity =" />
                                <Label Grid.Column="3" Text="{Binding Quantity}" />
                                <Button Grid.Column="4" Text="+" Command="{Binding AddCommand}" Clicked="InLineAddClicked"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Grid.Row="1"  Text="Add Item" Clicked="AddToCartClicked" />
            <Button Grid.Row="2" Text ="Go Back" Clicked="CancelClicked" />
        </Grid>
        
        <!--USER SHOPPING CART-->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ListView Grid.Row="0"
                      ItemsSource="{Binding ShoppingCart}"
                      SelectedItem="{Binding SelectedCartItem}" >
            <!--<ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Model.Display}" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>-->
            </ListView>
            <Button Grid.Row="1" Text="Return Item" Clicked="RemoveFromCartClicked"/>
        </Grid>
    </Grid>
</ContentPage>



























































